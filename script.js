const gridSize = 800;

const grid = document.querySelector("container");



for (let i = 0; i < 16; i++) {
    const square = document.createElement("div");
    grid.appendChild(square);

    let squareSize = gridSize / 
}